<div class="pages-nav">
    <ul>
        {{#each layers as |layer index|}}
            {{#if layer.settings.properties.showInNavigation}}
                <li>
                    <button class="btn btn-link" {{action 'scrollToLayer' index}}>{{layer.sectionHeader}}</button>
                </li>
            {{/if}}
        {{/each}}
    </ul>
</div>

