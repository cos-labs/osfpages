{{edit-text showSettings=showSettings editMode=editMode layer=layer type="title"}}

{{#if editMode}}
    <div class="m-t-md">
        {{quill-editor value=layer.content.settings.content options=options textChange=(action "updateText")}}
    </div>
{{else}}
    <div class="ql-editor">
        {{{layer.content.settings.content}}}
    </div>
{{/if}}