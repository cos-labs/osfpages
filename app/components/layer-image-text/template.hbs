{{edit-text showSettings=showSettings editMode=editMode layer=layer type="title"}}

<div style="margin:25px">
	<div>
		<img align={{layer.settings.values.alignment}} class="{{layer.settings.values.alignment}}" src="{{layer.settings.values.image}}">
	</div>
	<div style="margin-bottom:25px;">
    {{#if editMode}}
        {{textarea class="edit-text" rows="6" cols="50" value=layer.settings.values.sectionDescription}}
    {{else}}
        {{layer.settings.values.sectionDescription}}
    {{/if}}
	</div>
</div>