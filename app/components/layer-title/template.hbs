<div> 
        <a class="pull-left" href="#"{{action toggleProperty 'editMode'}} title="Toggle Edit Mode" style="color:{{theme.color}}">{{fa-icon "edit" size="lg"}}</a><br/>

    {{#if layer.settings.properties.showTitle}}
         <h1>{{node.title}}</h1>
    {{/if}}

    <div class="">

	    {{#if layer.settings.properties.showLinks}}
	        {{pages-menu layers=layers theme=theme}}
	    {{/if}}
    </div>
</div>

