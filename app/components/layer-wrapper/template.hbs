<div class="layer-wrapper {{layer.component}} theme-{{theme.type}}" style="background-color:{{theme.background}}; color: {{theme.color}}" >
    <div class="container">
        <div class="row">
            <div class="col-xs-12">
                <div class="layer clearfix">
                    <div class="layer-content">
                        {{component layer.component layer=layer node=node layers=layers}}
                        {{#unless editMode}}
                            <button class="btn btn-default" {{action toggleProperty 'showSettings'}}>Edit section</button>
                        {{/unless}}
                    </div>
                    {{#if showSettings}}
                        <div class="layer-settings">
                            <button type="button" class="close" aria-label="Close" {{action toggleProperty 'showSettings'}} ><span aria-hidden="true">&times;</span></button>
                            {{component layer.settings.component layer=layer themes=themes theme=theme}}
                        </div>
                    {{/if}}
                </div>
                {{#if (gt index 0) }}
                    <div class="layer-order">
                        <button {{action 'moveBefore' index}}> {{fa-icon 'angle-up'}} </button>
                        <button {{action 'moveAfter' index}}>{{fa-icon 'angle-down'}}</button>
                    </div>
                {{/if}}
            </div>
        </div>
    </div>
</div>
